<div class="album py-5 bg-light">
  <div class="container">
    <div class="row" id="<%= "sortable-projects" if logged_in?(:site_admin) %>" data-url="<%= sort_projects_path %>">
      <%= render partial: 'portfolio_project', collection: @portfolio_projects %>
    </div>
  </div>
</div>